in sourcetree under branches i pick a label and checkout that directory
then my working directory will have only those files.

if i switch to dev2creatredfeutrebr the i will see all the files.
let me add a few files to it as well
